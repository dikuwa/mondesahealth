"use client";
import { useState } from "react";
import { toast } from "sonner";
export function BookingMode({initial}:{initial:string}){const[mode,setMode]=useState(initial);async function save(){const r=await fetch("/api/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({bookingMode:mode})});if(r.ok)toast.success("Booking mode updated");else toast.error("Could not save booking mode");}return <div className="card" style={{padding:24}}><h2 style={{marginTop:0}}>Public booking mode</h2><p style={{fontSize:13,color:"#687a74"}}>Choose whether patients see live appointment times or submit a preferred-date request.</p><div style={{display:"grid",gap:10,margin:"20px 0"}}>{[["AVAILABLE_TIME","Show live available times"],["APPOINTMENT_REQUEST","Accept appointment requests"]].map(([value,label])=><label key={value} style={{border:`1px solid ${mode===value?"#1f5a4c":"#dce4df"}`,padding:15,borderRadius:11,display:"flex",gap:10}}><input type="radio" checked={mode===value} onChange={()=>setMode(value)}/><b>{label}</b></label>)}</div><button className="btn btn-primary" onClick={save}>Save booking mode</button></div>}
