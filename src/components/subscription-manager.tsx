"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Plus}from"lucide-react";import toast from"react-hot-toast";import{CustomSelect}from"@/components/ui/custom-select";export function SubscriptionManager({plans}:{plans:{id:string;name:string;description:string|null;billingFrequency:string;fee:number;gracePeriodDays:number;active:boolean}[]}){const router=useRouter(),[open,setOpen]=useState(false),[frequency,setFrequency]=useState("MONTHLY"),[saving,setSaving]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget),toastId=toast.loading("Creating plan…");try{const response=await fetch("/api/platform/subscriptions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),description:form.get("description"),billingFrequency:frequency,fee:Number(form.get("fee")),gracePeriodDays:Number(form.get("gracePeriodDays"))})}),data=await response.json();if(!response.ok)throw new Error(data.error);toast.success("Subscription plan created",{id:toastId});setOpen(false);router.refresh()}catch(e){toast.error(e instanceof Error?e.message:"Could not create plan",{id:toastId})}finally{setSaving(false)}}return <><div className="manager-toolbar"><div><h2>Subscription plans</h2><p>Plans restrict selected new actions only; clinical data is never deleted for billing status.</p></div><button className="btn btn-primary" onClick={()=>setOpen(true)}><Plus size={16}/> Create plan</button></div><div className="card dashboard-card"><div className="table-scroll"><table className="data-table"><thead><tr><th>Plan</th><th>Frequency</th><th>Fee</th><th>Grace period</th><th>Status</th></tr></thead><tbody>{plans.map(x=><tr key={x.id}><td><b>{x.name}</b><small style={{display:"block"}}>{x.description}</small></td><td>{x.billingFrequency}</td><td>N${x.fee.toFixed(2)}</td><td>{x.gracePeriodDays} days</td><td>{x.active?"Active":"Inactive"}</td></tr>)}</tbody></table></div></div>{open&&<div className="appointment-modal"><button className="appointment-modal-backdrop" onClick={()=>setOpen(false)}/><form className="appointment-panel" onSubmit={submit}><h2>Create subscription plan</h2><div className="form-grid"><label><span>Name</span><input className="input" name="name" required/></label><label><span>Billing frequency</span><CustomSelect value={frequency} onChange={setFrequency} options={["MONTHLY","QUARTERLY","ANNUAL","FIXED"].map(value=>({value,label:value}))}/></label><label><span>Fee (NAD)</span><input className="input" name="fee" type="number" min="0" step="0.01" required/></label><label><span>Grace period days</span><input className="input" name="gracePeriodDays" type="number" min="0" max="90" defaultValue="7" required/></label><label className="field-span-2"><span>Description</span><textarea className="input" name="description"/></label></div><div className="form-actions"><button type="button" className="btn btn-light" onClick={()=>setOpen(false)}>Cancel</button><button className="btn btn-primary" disabled={saving}>{saving?"Saving…":"Create plan"}</button></div></form></div>}</>}
