"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import toast from "react-hot-toast";
import { CustomSelect } from "@/components/ui/custom-select";
import { ConfirmationDialog } from "@/components/ui/confirmation-dialog";

type Grant = { id: string; practice: string; patientNumber: string; reason: string; expiresAt: string; revokedAt: string | null };
export function SupportAccessManager({ practices, grants }: { practices: { id: string; name: string }[]; grants: Grant[] }) { const router = useRouter(); const [practiceId,setPracticeId]=useState(practices[0]?.id||""),[duration,setDuration]=useState("15"),[saving,setSaving]=useState(false),[revoke,setRevoke]=useState<Grant|null>(null); async function create(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget),id=toast.loading("Creating restricted support grant…");try{const response=await fetch("/api/platform/support",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({practiceId,patientNumber:form.get("patientNumber"),reason:form.get("reason"),durationMinutes:Number(duration)})}),data=await response.json();if(!response.ok)throw new Error(data.error);toast.success("Time-limited support access granted",{id});router.push(`/platform/support/${data.id}`);router.refresh()}catch(error){toast.error(error instanceof Error?error.message:"Could not create support grant",{id})}finally{setSaving(false)}}async function confirmRevoke(){if(!revoke)return;setSaving(true);const id=toast.loading("Revoking support access…");try{const response=await fetch(`/api/platform/support?id=${revoke.id}`,{method:"DELETE"}),data=await response.json();if(!response.ok)throw new Error(data.error);toast.success("Support access revoked",{id});setRevoke(null);router.refresh()}catch(error){toast.error(error instanceof Error?error.message:"Could not revoke access",{id})}finally{setSaving(false)}}return <><form className="card dashboard-card" onSubmit={create}><div className="manager-toolbar"><div><h2>Request exceptional support access</h2><p>Use only for a documented support case. Access is patient-specific, read-only, time-limited, and audited.</p></div></div><div className="form-grid"><label className="field"><span>Practice</span><CustomSelect value={practiceId} onChange={setPracticeId} options={practices.map(x=>({value:x.id,label:x.name}))}/></label><label className="field"><span>Exact patient reference</span><input className="input" name="patientNumber" required/></label><label className="field"><span>Duration</span><CustomSelect value={duration} onChange={setDuration} options={[5,15,30,60].map(x=>({value:String(x),label:`${x} minutes`}))}/></label><label className="field field-span-2"><span>Documented reason</span><textarea className="input" name="reason" minLength={10} required/></label></div><button className="btn btn-primary" disabled={saving||!practiceId}>Create access grant</button></form><div className="card dashboard-card"><h2>Recent grants</h2><div className="record-stack">{grants.map(grant=><article className="record-row" key={grant.id}><div><b>{grant.practice} · {grant.patientNumber}</b><small>{grant.reason}</small><small>Expires {new Date(grant.expiresAt).toLocaleString("en-NA")}{grant.revokedAt?" · Revoked":""}</small></div><div className="table-actions">{!grant.revokedAt&&new Date(grant.expiresAt)>new Date()&&<><a className="btn btn-light" href={`/platform/support/${grant.id}`}>Open read-only</a><button className="btn btn-light" onClick={()=>setRevoke(grant)}>Revoke</button></>}</div></article>)}</div></div><ConfirmationDialog open={Boolean(revoke)} title="Revoke support access?" description="The patient-specific support grant will stop working immediately." confirmLabel="Revoke access" danger busy={saving} onCancel={()=>setRevoke(null)} onConfirm={confirmRevoke}/></>}
