"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Loader2, Plus, WalletCards, X } from "lucide-react";
import toast from "react-hot-toast";
import { CustomSelect } from "@/components/ui/custom-select";
import { money } from "@/lib/utils";
type Patient={id:string;fullName:string};type Invoice={id:string;number:string;patient:string;total:number;paid:number;status:string};
const paymentMethods=["CASH","CARD","EFT","MEDICAL_AID_PAYMENT","EMPLOYER_PAYMENT","OTHER"].map(value=>({value,label:value.replaceAll("_"," ")}));
export function FinanceManager({patients,invoices}:{patients:Patient[];invoices:Invoice[]}){const router=useRouter();const[mode,setMode]=useState<"invoice"|"payment"|null>(null);const[selected,setSelected]=useState("");const[responsibility,setResponsibility]=useState("PATIENT_FULL");const[payer,setPayer]=useState("PATIENT");const[method,setMethod]=useState("EFT");const[saving,setSaving]=useState(false);const invoice=invoices.find(item=>item.id===selected);
async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);setSaving(true);const toastId=toast.loading(mode==="invoice"?"Creating invoice…":"Recording payment…");try{const rate=Number(form.get("amount"));const body=mode==="invoice"?{patientId:selected,responsibility,patientResponsibility:responsibility==="MEDICAL_AID_FULL"?0:rate,medicalAidResponsibility:responsibility==="MEDICAL_AID_FULL"?rate:0,lines:[{description:form.get("description"),tariffCode:"",quantity:1,rate,patientPortion:responsibility==="MEDICAL_AID_FULL"?0:rate,medicalAidPortion:responsibility==="MEDICAL_AID_FULL"?rate:0}]}:{invoiceId:selected,amount:rate,method,payer,notes:form.get("notes")};const response=await fetch(mode==="invoice"?"/api/invoices":"/api/payments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.error);toast.success(mode==="invoice"?"Invoice created":"Payment and receipt recorded",{id:toastId});setMode(null);setSelected("");router.refresh()}catch(error){toast.error(error instanceof Error?error.message:"Could not save",{id:toastId})}finally{setSaving(false)}}
return <><div className="manager-toolbar"><div><h2>Invoices</h2><p>Create invoices, record payments, and issue receipts from one ledger.</p></div><button className="btn btn-primary" onClick={()=>{setSelected(patients[0]?.id||"");setMode("invoice")}}><Plus size={17}/> Create invoice</button></div><div className="card dashboard-card" style={{padding:20}}>{invoices.length?<div className="table-scroll"><table className="data-table"><thead><tr><th>Invoice</th><th>Patient</th><th>Total</th><th>Outstanding</th><th>Status</th><th>Actions</th></tr></thead><tbody>{invoices.map(item=><tr key={item.id}><td><b>{item.number}</b></td><td>{item.patient}</td><td>{money(item.total)}</td><td>{money(Math.max(0,item.total-item.paid))}</td><td>{item.status.replaceAll("_"," ")}</td><td><div className="table-actions"><button className="btn btn-light" disabled={item.paid>=item.total} onClick={()=>{setSelected(item.id);setMode("payment")}}><WalletCards size={15}/> Payment</button><a className="btn btn-light" href={`/api/documents/${item.id}/pdf`}>PDF</a></div></td></tr>)}</tbody></table></div>:<div className="dashboard-empty"><h3>No invoices yet</h3><p>Create an invoice after a consultation.</p></div>}</div>{mode&&<div className="appointment-modal" role="dialog" aria-modal="true"><button className="appointment-modal-backdrop" aria-label="Close" onClick={()=>setMode(null)}/><form className="appointment-panel" onSubmit={submit}><div className="appointment-panel-heading"><div><span className="eyebrow">Finance</span><h2>{mode==="invoice"?"Create invoice":"Record payment"}</h2><p>{mode==="payment"&&invoice?`${invoice.number} · ${money(invoice.total-invoice.paid)} outstanding`:"Create a single-line consultation invoice."}</p></div><button type="button" aria-label="Close" onClick={()=>setMode(null)}><X size={20}/></button></div><div className="appointment-form-grid">{mode==="invoice"&&<><div className="field dashboard-span-all"><label>Patient</label><CustomSelect value={selected} onChange={setSelected} options={patients.map(item=>({value:item.id,label:item.fullName}))}/></div><div className="field"><label>Responsibility</label><CustomSelect value={responsibility} onChange={setResponsibility} options={[{value:"PATIENT_FULL",label:"Patient"},{value:"MEDICAL_AID_FULL",label:"Medical aid"}]}/></div><div className="field"><label>Description</label><input className="input" name="description" defaultValue="Consultation" required/></div></>}{mode==="payment"&&<><div className="field"><label>Payer</label><CustomSelect value={payer} onChange={setPayer} options={[{value:"PATIENT",label:"Patient"},{value:"MEDICAL_AID",label:"Medical aid"},{value:"EMPLOYER",label:"Employer"},{value:"OTHER",label:"Other"}]}/></div><div className="field"><label>Method</label><CustomSelect value={method} onChange={setMethod} options={paymentMethods}/></div><div className="field dashboard-span-all"><label>Note</label><input className="input" name="notes"/></div></>}<div className="field"><label>Amount (NAD)</label><input className="input" name="amount" type="number" min="0.01" step="0.01" defaultValue={mode==="payment"&&invoice?Math.max(0,invoice.total-invoice.paid):undefined} required/></div></div><div className="appointment-panel-actions"><button type="button" className="btn btn-light" onClick={()=>setMode(null)}>Cancel</button><button className="btn btn-primary" disabled={saving||!selected}>{saving&&<Loader2 className="toast-spinner" size={17}/>} Save</button></div></form></div>}</>}
