"use client";
import { useEffect,useState } from "react";
import { useParams,useRouter } from "next/navigation";
import toast from "react-hot-toast";
export default function InvitationPage(){const{token}=useParams<{token:string}>(),router=useRouter(),[detail,setDetail]=useState<{name:string;email:string;practice:string}|null>(null),[error,setError]=useState(""),[saving,setSaving]=useState(false);useEffect(()=>{fetch(`/api/invitations/${token}`).then(async r=>{const data=await r.json();if(!r.ok)throw new Error(data.error);setDetail(data)}).catch(e=>setError(e.message))},[token]);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget);try{const response=await fetch(`/api/invitations/${token}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:form.get("password")})}),data=await response.json();if(!response.ok)throw new Error(data.error);toast.success("Account activated");router.push("/login")}catch(e){toast.error(e instanceof Error?e.message:"Could not activate account")}finally{setSaving(false)}}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><span className="eyebrow">Practice invitation</span><h1>{detail?`Join ${detail.practice}`:"Set up your account"}</h1>{error?<p className="notice-warning">{error}</p>:<><p>{detail?`${detail.name}, create a secure password for ${detail.email}.`:"Checking your invitation…"}</p>{detail&&<label><span>Password</span><input className="input" name="password" type="password" autoComplete="new-password" required minLength={12}/></label>}<button className="btn btn-primary" disabled={!detail||saving}>{saving?"Activating…":"Activate account"}</button></>}</form></main>}
