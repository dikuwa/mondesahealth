import { endOfDay, startOfDay } from "date-fns";
import { PageHeading, QuickActions, Stat, Status } from "@/components/dashboard";
import { db } from "@/lib/db";
import { money } from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function Overview(){const today=new Date();const [appointments,pending,claims,balances,payments]=await Promise.all([db.appointment.findMany({where:{startAt:{gte:startOfDay(today),lte:endOfDay(today)}},include:{patient:true},orderBy:{startAt:"asc"}}),db.appointment.count({where:{status:{in:["NEW_REQUEST","PENDING_CONFIRMATION","RESCHEDULE_PROPOSED"]}}}),db.claim.count({where:{status:{in:["MISSING_INFORMATION","REJECTED","RESUBMISSION_REQUIRED"]}}}),db.invoice.aggregate({_sum:{total:true,patientPaid:true,medicalAidPaid:true},where:{status:{not:"VOID"}}}),db.payment.aggregate({_sum:{amount:true},where:{paidAt:{gte:startOfDay(today),lte:endOfDay(today)}}})]);const outstanding=(balances._sum.total||0)-(balances._sum.patientPaid||0)-(balances._sum.medicalAidPaid||0);return <><PageHeading eyebrow="Monday · Practice overview" title="Good morning" action={<QuickActions/>}/><div className="dashboard-stats"><Stat label="Today's appointments" value={appointments.length}/><Stat label="Pending requests" value={pending}/><Stat label="Claims needing attention" value={claims}/><Stat label="Outstanding balances" value={money(outstanding)}/><Stat label="Received today" value={money(payments._sum.amount||0)}/></div><div className="dashboard-two-column"><section className="card dashboard-card" style={{padding:22}}><h2 style={{fontSize:17,margin:"0 0 18px"}}>Today’s agenda</h2>{appointments.length?<div style={{display:"grid"}}>{appointments.map(a=><div key={a.id} className="agenda-row"><b>{a.startAt?.toLocaleTimeString("en-NA",{hour:"2-digit",minute:"2-digit"})}</b><div><b style={{fontSize:14}}>{a.patient.fullName}</b><small style={{display:"block",color:"#6d7f79",marginTop:3}}>{a.patient.phone} · {a.reason}</small></div><Status value={a.status}/></div>)}</div>:<p style={{padding:35,textAlign:"center",color:"#71827c"}}>No appointments scheduled for today.</p>}</section><section className="card dashboard-card" style={{padding:22}}><h2 style={{fontSize:17,margin:"0 0 18px"}}>Practice pulse</h2><div style={{background:"#dcece6",borderRadius:15,padding:20}}><small>Next available appointment</small><b style={{display:"block",fontSize:25,letterSpacing:"-.035em",marginTop:8}}>Tomorrow · 08:00</b></div><p style={{fontSize:13,lineHeight:1.7,color:"#63766f"}}>Availability is based on working hours, lunch breaks, existing bookings and blocked time.</p></section></div></>}
