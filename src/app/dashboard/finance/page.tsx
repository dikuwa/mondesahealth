import { PageHeading, Stat } from "@/components/dashboard";
import { FinanceManager } from "@/components/finance-manager";
import { db } from "@/lib/db";
import { money } from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function Finance(){const[invoices,patients]=await Promise.all([db.invoice.findMany({include:{patient:true,payments:true},orderBy:{createdAt:"desc"}}),db.patient.findMany({select:{id:true,fullName:true},orderBy:{fullName:"asc"}})]);const total=invoices.reduce((n,i)=>n+i.total,0),paid=invoices.reduce((n,i)=>n+i.patientPaid+i.medicalAidPaid,0);return <><PageHeading eyebrow="Billing & payments" title="Finance"/><div className="dashboard-stats" style={{marginBottom:18}}><Stat label="Total invoiced" value={money(total)}/><Stat label="Payments received" value={money(paid)}/><Stat label="Patient outstanding" value={money(invoices.reduce((n,i)=>n+Math.max(0,i.patientResponsibility-i.patientPaid),0))}/><Stat label="Medical aid outstanding" value={money(invoices.reduce((n,i)=>n+Math.max(0,i.medicalAidResponsibility-i.medicalAidPaid),0))}/></div><FinanceManager patients={patients} invoices={invoices.map(i=>({id:i.id,number:i.number,patient:i.patient.fullName,total:i.total,paid:i.patientPaid+i.medicalAidPaid,status:i.status}))}/></>}
