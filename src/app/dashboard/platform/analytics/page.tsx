import{notFound}from"next/navigation";import{PageHeading,Stat}from"@/components/dashboard";import{requirePlatformOwner}from"@/lib/auth";import{db}from"@/lib/db";export default async function Analytics(){if(!await requirePlatformOwner())notFound();const[practices,active,appointments,patients]=await Promise.all([db.practice.count(),db.practice.count({where:{status:"ACTIVE"}}),db.appointment.count(),db.patient.count()]);return <><PageHeading eyebrow="Platform administration" title="Platform analytics"/><div className="dashboard-stats"><Stat label="Registered practices" value={practices}/><Stat label="Active practices" value={active}/><Stat label="Appointments" value={appointments}/><Stat label="Practice-owned patient profiles" value={patients}/></div><p className="notice-info">Platform analytics expose aggregate operational counts only. Clinical note content is not available here.</p></>}
