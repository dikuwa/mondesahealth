import{notFound}from"next/navigation";import{PageHeading,Stat}from"@/components/dashboard";import{PracticeDetailManager}from"@/components/practice-detail-manager";import{requirePlatformOwner}from"@/lib/auth";import{db}from"@/lib/db";export default async function PracticeDetail({params}:{params:Promise<{id:string}>}){if(!await requirePlatformOwner())notFound();const{id}=await params;const practice=await db.practice.findUnique({where:{id},include:{_count:{select:{users:true,patients:true,appointments:true,services:true,providers:true}},subscriptions:{include:{plan:true},orderBy:{createdAt:"desc"},take:1}}});if(!practice)notFound();return <><PageHeading eyebrow="Platform practice management" title={practice.name}/><div className="dashboard-stats"><Stat label="Staff" value={practice._count.users}/><Stat label="Patients" value={practice._count.patients}/><Stat label="Appointments" value={practice._count.appointments}/><Stat label="Services" value={practice._count.services}/><Stat label="Providers" value={practice._count.providers}/><Stat label="Subscription" value={practice.subscriptions[0]?.plan.name||"Unassigned"}/></div><PracticeDetailManager practice={practice}/></>}
