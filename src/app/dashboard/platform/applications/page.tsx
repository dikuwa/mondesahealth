import{notFound}from"next/navigation";import{PageHeading}from"@/components/dashboard";import{ProviderApplicationsManager}from"@/components/provider-applications-manager";import{requirePlatformOwner}from"@/lib/auth";import{db}from"@/lib/db";export default async function Applications(){if(!await requirePlatformOwner())notFound();const[applications,plans]=await Promise.all([db.practiceApplication.findMany({orderBy:{createdAt:"desc"}}),db.subscriptionPlan.findMany({where:{active:true},orderBy:{fee:"asc"}})]);return <><PageHeading eyebrow="Platform administration" title="Provider applications"/><ProviderApplicationsManager applications={applications.map(x=>({...x,createdAt:x.createdAt.toISOString()}))} plans={plans.map(({id,name})=>({id,name}))}/></>}
