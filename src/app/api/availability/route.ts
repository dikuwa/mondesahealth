import { NextResponse } from "next/server";
import { z } from "zod";
import { requirePermission } from "@/lib/auth";
import { db } from "@/lib/db";
async function allowed(){return requirePermission("MANAGE_AVAILABILITY")}
export async function PATCH(request:Request){const session=await allowed();if(!session)return NextResponse.json({error:"You do not have permission to manage availability."},{status:403});const parsed=z.object({rules:z.array(z.object({weekday:z.number().int().min(0).max(6),active:z.boolean(),openTime:z.string().regex(/^\d{2}:\d{2}$/),closeTime:z.string().regex(/^\d{2}:\d{2}$/),durationMinutes:z.number().int().min(10).max(180)})).length(7)}).safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Check the weekly hours."},{status:400});const weekdays=new Set(parsed.data.rules.map(rule=>rule.weekday));if(weekdays.size!==7||parsed.data.rules.some(rule=>rule.active&&rule.closeTime<=rule.openTime))return NextResponse.json({error:"Each active day needs valid opening and closing times."},{status:400});await db.$transaction([...parsed.data.rules.map(rule=>db.availabilityRule.upsert({where:{weekday:rule.weekday},update:rule,create:rule})),db.activityLog.create({data:{userId:session.id,action:"AVAILABILITY_UPDATED",entityType:"AvailabilityRule",entityId:"weekly",summary:"Weekly availability rules updated"}})]);return NextResponse.json({ok:true})}
export async function POST(request:Request){const session=await allowed();if(!session)return NextResponse.json({error:"You do not have permission to manage availability."},{status:403});const parsed=z.object({startAt:z.string().datetime(),endAt:z.string().datetime(),reason:z.string().trim().min(2).max(200)}).safeParse(await request.json());if(!parsed.success||new Date(parsed.data.endAt)<=new Date(parsed.data.startAt))return NextResponse.json({error:"Enter a valid start and end time."},{status:400});const block=await db.blockedTime.create({data:{startAt:new Date(parsed.data.startAt),endAt:new Date(parsed.data.endAt),reason:parsed.data.reason}});await db.activityLog.create({data:{userId:session.id,action:"BLOCKED_TIME_CREATED",entityType:"BlockedTime",entityId:block.id,summary:`Blocked time added: ${parsed.data.reason}`}});return NextResponse.json(block,{status:201})}
export async function DELETE(request:Request){const session=await allowed();if(!session)return NextResponse.json({error:"You do not have permission to manage availability."},{status:403});const id=new URL(request.url).searchParams.get("id");if(!id)return NextResponse.json({error:"Blocked time is required."},{status:400});const result=await db.blockedTime.deleteMany({where:{id}});if(!result.count)return NextResponse.json({error:"Blocked time was already removed."},{status:404});await db.activityLog.create({data:{userId:session.id,action:"BLOCKED_TIME_REMOVED",entityType:"BlockedTime",entityId:id,summary:"Blocked time removed"}});return NextResponse.json({ok:true})}
