"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Loader2, LockKeyhole } from "lucide-react";
import toast from "react-hot-toast";
export default function Login(){const router=useRouter();const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});const d=await r.json();setLoading(false);if(!r.ok)return toast.error(d.error);router.push("/dashboard");router.refresh();}return <main style={{minHeight:"75vh",display:"grid",placeItems:"center",background:"#f7f4ed",padding:30}}><form onSubmit={submit} className="card" style={{width:"min(440px,100%)",padding:38}}><span style={{width:48,height:48,borderRadius:14,display:"grid",placeItems:"center",background:"#dcece6"}}><LockKeyhole/></span><h1 className="display" style={{fontSize:42,margin:"22px 0 8px"}}>Staff sign in</h1><p style={{color:"#63756f",fontSize:14,marginBottom:25}}>Access is restricted to authorised Mondesa Health staff.</p><div className="field"><label>Email</label><input name="email" type="email" className="input" defaultValue="owner@mondesahealth.na" required/></div><div className="field" style={{marginTop:16}}><label>Password</label><input name="password" type="password" className="input" defaultValue="Mondesa2026!" required/></div><button className="btn btn-primary" disabled={loading} style={{width:"100%",marginTop:24}}>{loading?<Loader2 size={18}/>:null} Sign in</button><p style={{fontSize:11,color:"#8a8175",textAlign:"center",marginTop:18}}>Demo credentials are pre-filled for local evaluation. Change them before deployment.</p></form></main>}
